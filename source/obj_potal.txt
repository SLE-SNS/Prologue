/// @description 여기에 설명 삽입
// 이 에디터에 코드를 작성할 수 있습니다
// 코드 복사 붙여넣기 할거면 각 인스턴스별로 하는 것보다는 이 방식이 나을 것 같아서 선택
//오브젝트와 부딪히면 가도록 함
// 룸과 오브젝트 명에 따라 변경가능하도록 함

if(room == villeage){
	room_goto(forest1);
	objchop.x = 73;
	objchop.y = 154;
}
else if (room == forest1){
	room_goto(villeage);
	objchop.x = 25;
	objchop.y = 90;
}

